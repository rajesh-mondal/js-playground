const result = Math.pow(3, 2);
console.log(result);

/* round */
const number = 2.45598;
const fullNumber = Math.round(number);
console.log(fullNumber);

/* ceiling and floor */
const resultCeil = Math.ceil(2.2121);
const resultFloor = Math.floor(2.2121);
console.log(resultFloor);

/* random */
console.log(Math.random());
// for random number from 1 to 100
const random = Math.round(Math.random()*100);
console.log(random);